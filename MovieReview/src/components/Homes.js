import React, {useEffect} from 'react';
import { SaveAreaView, StatusBar, Text, View, StyleSheet } from 'react-native';

function Homes() {
  return (
    <SaveAreaView style={{flex: 1, backgroundColor: '#343434', alignItem: "center", justifyContent: "center"}}>
      <Text>YEY</Text>
    </SaveAreaView>
  )
};

export default Homes;
